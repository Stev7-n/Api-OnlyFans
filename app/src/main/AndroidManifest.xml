<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pngonly"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pngonly"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApiOnlyFans"
        tools:targetApi="31">
        <activity
            android:name=".perfil_ajustes"
            android:exported="false" />
        <activity
            android:name=".subir_posts"
            android:exported="false" />
        <activity
            android:name=".sugerencias_perfiles"
            android:exported="false" />
        <activity
            android:name=".menu_inicial"
            android:exported="false" />
        <activity
            android:name=".registrarse_only"
            android:exported="false" />
        <activity
            android:name=".iniciar_sesion"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>